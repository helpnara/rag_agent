# 개발 진행상황 및 향후 계획

> 이 문서는 일자별 작업 내용과 다음에 이어서 할 일을 정리한다.
> 상세 요구사항은 `사내문서_RAG_요구사항정의서.md`(SRS), 작업 규칙은 `CLAUDE.md` 참조.
> 최종 갱신: 2026-07-17

---

## 오늘 진행상황 (2026-07-17)

### 1. 기존 작업물 저장소 반입
- 그동안 작업한 RAG 에이전트 웹 애플리케이션(`rag-agent-web4`)을 `rag_agent` 저장소에 반입.
- FastAPI 백엔드(`app/`), 단일 파일 웹 UI(`static/index.html`), 오프라인 반입 스크립트, 문서 일체 포함.
- 커밋: `Initial commit: RAG agent web application`

### 2. LLM 이중 모드(로컬/외부) 도입 — SRS v4.2
온프레미스(로컬 Ollama)와 외부 생성형 LLM API(OpenAI 호환) 간 전환 기능 추가.

**설계 원칙**
- 기본값은 **완전 로컬**. 외부 모드는 API 키가 설정된 경우에만 켜지는 **옵트인** 구조.
- **임베딩은 두 모드 모두 로컬 bge-m3 유지** (기존 색인과의 벡터공간 호환).
- 외부 모드 선택 시 "질문 + 문서 컨텍스트 외부 전송" **경고 배너** 표시.

**백엔드**
- `config.py`: `LLM_MODE`, `EXTERNAL_BASE_URL/API_KEY/MODEL`, `external_configured()` 추가.
- `chat_engine.py`: `_llm()`이 모드에 따라 `ChatOllama` ↔ `ChatOpenAI` 선택(외부 의존성 지연 임포트).
- `models_util.py`: `list_external_models()` — OpenAI 호환 `/models` 조회 + 폴백.
- `main.py`: `/api/models` 모드별 목록 반환, `ChatRequest.mode`, `/api/status` 확장.
- `requirements.txt`: `langchain-openai==0.2.9` 추가.

**프론트엔드(`static/index.html`)**
- 모드 전환 콤보박스 + 현재 모드 뱃지 + 외부 전송 경고 배너.
- 미설정 시 외부 옵션 비활성화, 모드별 모델 목록 동적 갱신.

**문서**
- SRS를 v4.2로 갱신(FR-LLM-01~06, FR-UI-07~08, NFR-SEC-06, 변경이력).
- `CLAUDE.md` 절대원칙 1을 "기본 로컬 / 외부는 선택적 옵트인"으로 개정.

**검증 수준**
- 변경 Python 파일 byte-compile 통과.
- `config`/`models_util` 로직 단위 테스트 3케이스 통과(미설정→비활성, 설정+조회실패→폴백, 환경변수 반영).
- ⚠️ **미완**: 무거운 런타임 의존성(langchain, fastapi, ollama) 미설치로 **서버 기동 end-to-end 검증은 못 함**. 실제 외부 API 연결 확인 필요(아래 향후 계획 참조).

---

## 2일차 (2026-07-18) — A. 이중 모드 실동작 검증 ✅ 완료

### 검증 방법
Ollama·Qdrant·외부 API가 없는 환경이므로 **목(mock) 서버 방식**으로 검증했다.
- 목 Ollama(`/api/tags`, `/api/chat` NDJSON 스트리밍)와 목 OpenAI 호환 서버
  (`/v1/models`, `/v1/chat/completions` SSE)를 띄워 **실제 HTTP 스트리밍 경로를 그대로** 태움.
- 임베딩·벡터DB는 모드 분기와 무관하므로 `app.retrieval`만 스텁으로 대체.
- 백엔드는 FastAPI 실제 호출, UI는 **Chromium(Playwright)으로 실제 브라우저 조작**.
- 검증 하네스는 `tests/`에 포함 → 폐쇄망에서도 재실행 가능(추가 의존성 없음).

### 결과: 총 60개 항목 전부 통과
| 구분 | 항목 수 | 내용 |
|------|--------|------|
| 백엔드 | 32 | 모델목록·모드분기·스트리밍·오류처리·호환성·회귀 |
| UI(정상 설정) | 16 | 모드 전환·경고 배너·모델목록 갱신·채팅·다크모드 |
| UI(미설정) | 7 | 외부 옵션 비활성화·로컬 정상 동작 |
| UI(잘못된 키) | 5 | 오류 메시지 표시·입력창 복구 |

### 항목별 확인 내역
- [x] 서버 기동, **로컬 모드 정상 동작(회귀 없음)** — 스트리밍·출처·대화기억 모두 정상.
- [x] **외부 모드 실제 스트리밍 응답 확인** — OpenAI 호환 엔드포인트로 토큰 스트리밍 수신.
- [x] **UI 모드 전환·경고 배너·모델 목록 갱신** — 전환 시 뱃지/배너/모델목록이 즉시 갱신됨.
- [x] **외부 모드 예외 처리** — 잘못된 키(401)·연결 실패·키 미설정 모두 `error` 이벤트로
      안전 반환, 서버 크래시 없음. 화면에 오류 메시지 표시되고 입력창도 정상 복구.
- [x] **OpenAI 외 호환 서버 호환성** — 자체 호스팅 OpenAI 호환 엔드포인트로 정상 동작
      (vLLM 등 대체 가능). base_url 끝 슬래시도 안전 처리.

### 보안 관련 핵심 확인 (설계 의도대로 동작함을 실증)
- ★ **로컬 모드에서는 외부 API 호출이 단 한 건도 발생하지 않음** (NFR-SEC-01).
- ★ **키 미설정 시 외부로 아무것도 전송되지 않음** — 외부 옵션 자체가 UI에서 비활성화(`미설정` 표기).
- ★ **외부 모드에서는 질문 + 문서 컨텍스트가 실제로 전송됨을 목 서버 수신 내용으로 확인**
  → UI 경고 배너 문구가 과장이 아니라 **사실과 일치**함을 검증.

### 발견 사항 (이번 변경과 무관한 선재 이슈)
- **다크모드 잔여 라이트 스타일**: `.btn`(예: "새 대화")과 `.input-wrap`(입력창)이
  `background: #fff` 하드코딩이라 다크모드에서도 흰색으로 남는다.
  이번 이중 모드 작업 이전부터 있던 문제 → 아래 **BL-04**로 등록.
- 참고: 모드 콤보박스의 `☁`/`🖥` 이모지는 리눅스 컨테이너 폰트에서 흐리게 보였으나,
  실사용 환경(Windows/Segoe UI Emoji)에서는 정상 표시 예상. 기능 영향 없음.

### 남은 확인 (운영 환경에서 1회)
- [ ] 실제 Ollama + 실제 외부 API 키로 최종 연결 확인(목 서버가 아닌 실물).
- [ ] 실제 Qdrant + bge-m3 색인 상태에서 두 모드 답변 품질 비교.

---

## 3일차 (2026-07-31) — 공개 데모(Streamlit Cloud) 구축 ✅ 완료

### 개발 전략 확정
**공개 데모로 먼저 검증 → 이후 온프레미스 이식.** 하나의 저장소에서 구성만 바꿔
두 형태를 유지한다. 온프레미스가 기본값이며 데모는 환경변수로 명시적으로 켠다.

### Streamlit Cloud 제약과 대응
| 항목 | 온프레미스 | 데모 | 이유 |
|------|-----------|------|------|
| 임베딩 | bge-m3 1024차원 | fastembed 384차원 | bge-m3 약 2GB → Cloud 메모리 초과. fastembed는 ONNX라 torch 불필요 |
| 벡터DB | Qdrant Docker | qdrant-client 메모리 | Cloud에 Docker 없음 |
| LLM | Ollama 로컬 | 외부 OpenAI 호환 API | Cloud에서 로컬 LLM 구동 불가 |
| 의존성 | `requirements-onprem.txt` | `requirements.txt` | Streamlit↔FastAPI starlette 충돌 |

> 임베딩은 **두 구성 모두 로컬 실행**이다. 외부 임베딩 API는 도입하지 않았다.

### 구현
- `app/embeddings.py` 신규 — 임베딩 백엔드 추상화(bge-m3 / fastembed).
- `app/config.py` — `EMBEDDING_BACKEND`, `QDRANT_MODE/PATH`, `FASTEMBED_*`, `DEMO_*` 추가.
- `app/vectorstore.py` — Qdrant server/memory/path 전환, 차원 동적화,
  오프라인 강제는 온프레미스 백엔드에서만 적용.
- `app/chat_engine.py` — Ollama도 지연 임포트(데모에 ollama 패키지 불필요).
- `streamlit_app.py` 신규 — 데모 UI(채팅·출처·샘플질문·방문자 키·횟수 제한).
- `demo_docs/` 신규 — 가상 회사 샘플 문서 3종(인사규정·정보보안·경비처리).
- `requirements.txt`(데모) / `requirements-onprem.txt`(운영) 분리.
- `.streamlit/secrets.toml.example` 추가.

### 검증 결과: 67개 항목 전부 통과
| 구분 | 항목 수 | 비고 |
|------|--------|------|
| 온프레미스 회귀 (`verify_llm_modes.py`) | 32 | 기존 기능 무회귀 |
| 데모 구성 (`verify_demo_rag.py`) | 20 | 색인·검색·외부 스트리밍·온프레미스 기본값 유지 |
| 데모 UI (Chromium 실제 조작) | 15 | 경고·샘플질문·출처·횟수제한·본인키 전환 |

- 목 LLM 서버를 **표준 라이브러리 기반으로 재작성** → 두 환경 모두에서 실행 가능.
- 검증 중 발견해 수정한 실제 결함 3건:
  1. `FastEmbedEmbeddings`가 `Embeddings`를 상속하지 않아 langchain-qdrant가 거부.
  2. secrets 파일이 없을 때 사이드바에 "No secrets found" 오류 박스 노출.
  3. 출처 스니펫이 마크다운으로 렌더링돼 제목·표가 본문처럼 표시됨.

### ⚠️ 이 환경에서 검증하지 못한 것
- **실제 fastembed 모델 다운로드·임베딩 품질**: 컨테이너에서 `huggingface.co`가
  네트워크 정책으로 차단(403)되어 모델을 받을 수 없었다. 결정적 스텁 임베딩으로
  **배선(색인→검색→스트리밍)은 검증**했으나, 실제 모델 로드와 한국어 검색 품질은
  미검증이다. → 인터넷 되는 환경에서 `streamlit run streamlit_app.py`로 1회 확인 필요.
- **실제 외부 LLM API 연결**: 목 서버로만 검증. 실제 키로 1회 확인 필요.

### 마무리 시점 결정 (2026-07-31)
- GitHub 기본 브랜치를 `main`으로 변경 완료(확인함). Streamlit Cloud 배포 준비 상태.
- **다만 Streamlit Cloud에서의 테스트가 번거롭다고 판단** → 전략 조정:
  **① Windows 로컬에서 먼저 충분히 테스트 → ② 이후 Cloud 배포**.
  Cloud는 로그 확인·재배포 사이클이 느려 초기 디버깅에 불리하므로 합리적인 순서다.
- 이에 맞춰 README에 **Windows(PowerShell) 데모 실행 절차**를 추가했다.

---

## 향후 계획

### A0. VS Code + Claude Code 개발환경 (구성 완료)

로컬에서 테스트하며 Claude Code와 대화로 작업을 이어가기 위한 환경을 구성했다.
절차는 **`개발환경_VSCode_가이드.md`** 참조.

- `.vscode/launch.json` — F5 실행 구성 7종(FastAPI·색인·점검·검증·Streamlit).
  각 구성이 사용할 가상환경을 명시하므로 인터프리터 선택과 무관하게 동작한다.
- `.vscode/tasks.json` — 디버거 없이 실행하는 작업(설치·검증·실행), `검증: 전체` 포함.
- `.vscode/settings.json` — UTF-8 강제, 대용량 폴더 검색 제외, 인터프리터 기본값.
- `.vscode/extensions.json` — Claude Code·Python 확장 권장.

**이 과정에서 고친 실제 결함 1건**
- 검증 스크립트가 `✅`(U+2705)를 출력하는데 **한글 Windows 콘솔(cp949)에서는
  인코딩 불가 → `UnicodeEncodeError`로 즉시 중단**된다. 컨테이너(UTF-8)에서는
  드러나지 않던 문제. `verify_llm_modes.py`·`verify_demo_rag.py`에서 출력 스트림을
  UTF-8로 재설정하도록 수정했다(cp949 재현 후 수정 전/후 동작 확인).

### A0-1. Windows 실환경에서 드러난 결함 (수정 완료)

컨테이너(Linux/UTF-8)에서는 드러나지 않다가 실제 Windows에서 확인된 문제들.

| 증상 | 원인 | 조치 |
|------|------|------|
| 검증 스크립트가 `UnicodeEncodeError`로 중단 | `✅`(U+2705)를 cp949 콘솔이 인코딩 못 함 | 출력 스트림 UTF-8 재설정 |
| `ModuleNotFoundError: huggingface_hub` | 가상환경 미활성 | 실행 Python·활성 여부를 출력하는 안내 메시지 추가 |
| `Qdrant 연결 실패 (WinError 10061)` | Docker 없이 `server` 모드 | 안내에 `QDRANT_MODE=path` 대안 명시 |
| **`ImportError: DLL load failed ... 애플리케이션 제어 정책에서 차단`** | Windows 앱 제어 정책이 `_xxhash.pyd` 차단. `xxhash`는 `langsmith`(미사용 기능)의 의존성 | **`langsmith==0.4.31` 고정** (0.5+ uuid-utils, 0.6.7+ xxhash 추가) |

**langsmith 고정 근거**
- `langchain-core`가 요구하는 범위 `>=0.3.45,<1.0.0` 안에서 네이티브 의존성이
  가장 적은 계열이 0.4.x (orjson·zstandard만, xxhash·uuid-utils 없음).
- 이 프로젝트는 LangSmith 추적 기능을 쓰지 않으므로 기능 손실이 없다.
- 부수 효과로 **폐쇄망 반입 wheel 수가 줄어든다**.
- 검증: `sys.modules['xxhash']=None`으로 차단을 재현한 상태에서 온프레미스·데모
  임포트 사슬이 모두 통과함을 확인. 기존 검증도 무회귀(32/20 통과).

### A1. 개인 PC 온프레미스 시험 (가이드 작성 완료)

사내 반입 전에 개인 Windows PC에서 온프레미스 형태를 그대로 시험하는 절차를
README **「개인 PC 빠른 시작 (Windows · Docker 없이)」**에 정리했다.

**핵심: Docker 없이도 온프레미스 구성이 가능하다.**
`QDRANT_MODE=path`로 두면 벡터DB가 로컬 파일에 저장되고 재시작해도 유지된다.
(적재 6건 → 프로세스 재시작 → 재색인 없이 검색되는 것까지 확인)

**이 과정에서 고친 실제 결함 2건**
1. `DEVICE` 기본값이 `cuda`였다 → GPU 없는 PC에서 즉시 실패.
   CLAUDE.md·SRS(NFR-ENV-01)는 "GPU 없는 CPU 환경 전제"라고 명시하고 있어
   코드가 문서와 어긋난 상태였다. **기본값을 `cpu`로 수정**(GPU가 있으면 `DEVICE=cuda`).
2. `check_offline.py`가 Docker(server) 구성만 점검해 `path`/`memory` 모드에서
   잘못된 실패를 보고했다. **현재 설정(임베딩·벡터DB·LLM)에 맞춰 점검**하도록 수정.
   외부 LLM 모드일 때는 Ollama 대신 API 키 설정 여부를 점검한다.

**남은 확인 (개인 PC에서)**
- [ ] `pip install -r requirements-onprem.txt` (torch 포함) Windows에서 정상 완료.
- [ ] `python download_models.py`로 bge-m3(약 2GB) 다운로드.
- [ ] `python check_offline.py` 전 항목 통과.
- [ ] `python -m app.ingest` 색인 후 `uvicorn app.main:app --reload` 실행.
- [ ] CPU 추론 응답 속도 확인(느리면 `qwen2.5:3b`로 교체).

### A2. Windows 로컬 데모 테스트

**실행 (PowerShell)**
```powershell
git clone https://github.com/helpnara/rag_agent.git
cd rag_agent
py -3.11 -m venv .venv-demo
.\.venv-demo\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run streamlit_app.py          # → http://localhost:8501
```
> 온프레미스용 `.venv`와 **반드시 분리**한다(Streamlit↔FastAPI starlette 충돌).

**확인 항목**
- [ ] 패키지 설치가 Windows에서 오류 없이 끝나는지 (fastembed/onnxruntime 포함).
- [ ] **fastembed 모델(약 220MB) 다운로드 성공** — 이 환경에서 검증 못 한 최대 리스크.
      캐시 위치: `%USERPROFILE%\.cache\fastembed`
- [ ] 샘플 문서 3종이 자동 색인되고 사이드바에 청크 수가 표시되는지.
- [ ] **한국어 검색 품질** — "연차" 질문에 인사규정 문서가 상위로 올라오는지.
      (미흡하면 `FASTEMBED_MODEL`을 `paraphrase-multilingual-mpnet-base-v2`(768차원)로 상향 검토)
- [ ] 실제 API 키로 답변 스트리밍·출처 표시 정상 동작.
- [ ] 체험 횟수 제한 / 본인 키 입력 전환 동작.

**막히면**
- 키·모델 없이 배선만 점검: `python tests\verify_demo_rag.py`
  (모델을 못 받으면 스텁 임베딩으로 자동 대체되며, 마지막 줄에 어느 쪽인지 표시)

### A3. Streamlit Cloud 배포 (로컬 확인 후)
- [ ] Create app — Branch `main`, Main file path `streamlit_app.py`.
- [ ] Secrets에 `EXTERNAL_API_KEY` 설정(선택).
- [ ] Cloud 메모리 한도 내 정상 기동 확인.
- [ ] 배포 URL을 README에 기재.

### B. 이중 모드 후속 개선 (선택)
- [ ] 외부 모드에서도 사용자 예측 모델 Tool Calling 연동 동작 확인/보강.
- [ ] 외부 모드 모델 선택 시 응답 지연·비용에 대한 안내(선택적 UI 텍스트).
- [ ] 서버 로그에 외부 전송 이벤트 감사 로깅(보안 검토 필요).

### C. 기존 백로그 (SRS 6장 / CLAUDE.md)
- [ ] **BL-01 대화기억 영구보관**: `chat_engine.py`의 `_HISTORY`(서버 메모리) → 파일/DB 교체. 재시작 시 소실 해결.
- [ ] **BL-02 브라우저 테마·세션 저장**: 다크모드/세션을 localStorage 등에 저장(폐쇄망 보안 검토 후).
- [ ] **FR-TOOL-04 로컬 모델 Tool Calling 안정성**: docstring 정교화, 재시도 처리.
- [ ] **BL-03 답변 중단 시 서버측 생성 즉시 중지 검증**: GeneratorExit 처리 후 Ollama 생성이 실제로 멈추는지 확인.
- [ ] **BL-04 다크모드 잔여 라이트 스타일**: `.btn`·`.input-wrap`의 `background:#fff`
      하드코딩을 CSS 변수로 교체. (2일차 검증 중 발견, 선재 이슈)

---

## 참고: 현재 브랜치·상태
- 작업 브랜치: `claude/rag-agent-compress-commit-2bwx5x`
- **기본·배포 브랜치: `main`** (2026-07-31 변경 완료, Windows에서 clone 시 이 브랜치)
- SRS 최신 버전: **v4.3**
- 검증 재실행 (가상환경 분리 필요):
  - 온프레미스: `python tests/verify_llm_modes.py`
  - 데모: `python tests/verify_demo_rag.py`
- 다음 착수 지점: **A2. Windows 로컬 데모 테스트**부터 시작.
