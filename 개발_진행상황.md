# 개발 진행상황 및 향후 계획

> 이 문서는 일자별 작업 내용과 다음에 이어서 할 일을 정리한다.
> 상세 요구사항은 `사내문서_RAG_요구사항정의서.md`(SRS), 작업 규칙은 `CLAUDE.md` 참조.
> 최종 갱신: 2026-07-17

---

## 오늘 진행상황 (2026-07-17)

### 1. 기존 작업물 저장소 반입
- 그동안 작업한 RAG 에이전트 웹 애플리케이션(`rag-agent-web4`)을 `rag_agent` 저장소에 반입.
- FastAPI 백엔드(`app/`), 단일 파일 웹 UI(`static/index.html`), 오프라인 반입 스크립트, 문서 일체 포함.
- 커밋: `Initial commit: RAG agent web application`

### 2. LLM 이중 모드(로컬/외부) 도입 — SRS v4.2
온프레미스(로컬 Ollama)와 외부 생성형 LLM API(OpenAI 호환) 간 전환 기능 추가.

**설계 원칙**
- 기본값은 **완전 로컬**. 외부 모드는 API 키가 설정된 경우에만 켜지는 **옵트인** 구조.
- **임베딩은 두 모드 모두 로컬 bge-m3 유지** (기존 색인과의 벡터공간 호환).
- 외부 모드 선택 시 "질문 + 문서 컨텍스트 외부 전송" **경고 배너** 표시.

**백엔드**
- `config.py`: `LLM_MODE`, `EXTERNAL_BASE_URL/API_KEY/MODEL`, `external_configured()` 추가.
- `chat_engine.py`: `_llm()`이 모드에 따라 `ChatOllama` ↔ `ChatOpenAI` 선택(외부 의존성 지연 임포트).
- `models_util.py`: `list_external_models()` — OpenAI 호환 `/models` 조회 + 폴백.
- `main.py`: `/api/models` 모드별 목록 반환, `ChatRequest.mode`, `/api/status` 확장.
- `requirements.txt`: `langchain-openai==0.2.9` 추가.

**프론트엔드(`static/index.html`)**
- 모드 전환 콤보박스 + 현재 모드 뱃지 + 외부 전송 경고 배너.
- 미설정 시 외부 옵션 비활성화, 모드별 모델 목록 동적 갱신.

**문서**
- SRS를 v4.2로 갱신(FR-LLM-01~06, FR-UI-07~08, NFR-SEC-06, 변경이력).
- `CLAUDE.md` 절대원칙 1을 "기본 로컬 / 외부는 선택적 옵트인"으로 개정.

**검증 수준**
- 변경 Python 파일 byte-compile 통과.
- `config`/`models_util` 로직 단위 테스트 3케이스 통과(미설정→비활성, 설정+조회실패→폴백, 환경변수 반영).
- ⚠️ **미완**: 무거운 런타임 의존성(langchain, fastapi, ollama) 미설치로 **서버 기동 end-to-end 검증은 못 함**. 실제 외부 API 연결 확인 필요(아래 향후 계획 참조).

---

## 2일차 (2026-07-18) — A. 이중 모드 실동작 검증 ✅ 완료

### 검증 방법
Ollama·Qdrant·외부 API가 없는 환경이므로 **목(mock) 서버 방식**으로 검증했다.
- 목 Ollama(`/api/tags`, `/api/chat` NDJSON 스트리밍)와 목 OpenAI 호환 서버
  (`/v1/models`, `/v1/chat/completions` SSE)를 띄워 **실제 HTTP 스트리밍 경로를 그대로** 태움.
- 임베딩·벡터DB는 모드 분기와 무관하므로 `app.retrieval`만 스텁으로 대체.
- 백엔드는 FastAPI 실제 호출, UI는 **Chromium(Playwright)으로 실제 브라우저 조작**.
- 검증 하네스는 `tests/`에 포함 → 폐쇄망에서도 재실행 가능(추가 의존성 없음).

### 결과: 총 60개 항목 전부 통과
| 구분 | 항목 수 | 내용 |
|------|--------|------|
| 백엔드 | 32 | 모델목록·모드분기·스트리밍·오류처리·호환성·회귀 |
| UI(정상 설정) | 16 | 모드 전환·경고 배너·모델목록 갱신·채팅·다크모드 |
| UI(미설정) | 7 | 외부 옵션 비활성화·로컬 정상 동작 |
| UI(잘못된 키) | 5 | 오류 메시지 표시·입력창 복구 |

### 항목별 확인 내역
- [x] 서버 기동, **로컬 모드 정상 동작(회귀 없음)** — 스트리밍·출처·대화기억 모두 정상.
- [x] **외부 모드 실제 스트리밍 응답 확인** — OpenAI 호환 엔드포인트로 토큰 스트리밍 수신.
- [x] **UI 모드 전환·경고 배너·모델 목록 갱신** — 전환 시 뱃지/배너/모델목록이 즉시 갱신됨.
- [x] **외부 모드 예외 처리** — 잘못된 키(401)·연결 실패·키 미설정 모두 `error` 이벤트로
      안전 반환, 서버 크래시 없음. 화면에 오류 메시지 표시되고 입력창도 정상 복구.
- [x] **OpenAI 외 호환 서버 호환성** — 자체 호스팅 OpenAI 호환 엔드포인트로 정상 동작
      (vLLM 등 대체 가능). base_url 끝 슬래시도 안전 처리.

### 보안 관련 핵심 확인 (설계 의도대로 동작함을 실증)
- ★ **로컬 모드에서는 외부 API 호출이 단 한 건도 발생하지 않음** (NFR-SEC-01).
- ★ **키 미설정 시 외부로 아무것도 전송되지 않음** — 외부 옵션 자체가 UI에서 비활성화(`미설정` 표기).
- ★ **외부 모드에서는 질문 + 문서 컨텍스트가 실제로 전송됨을 목 서버 수신 내용으로 확인**
  → UI 경고 배너 문구가 과장이 아니라 **사실과 일치**함을 검증.

### 발견 사항 (이번 변경과 무관한 선재 이슈)
- **다크모드 잔여 라이트 스타일**: `.btn`(예: "새 대화")과 `.input-wrap`(입력창)이
  `background: #fff` 하드코딩이라 다크모드에서도 흰색으로 남는다.
  이번 이중 모드 작업 이전부터 있던 문제 → 아래 **BL-04**로 등록.
- 참고: 모드 콤보박스의 `☁`/`🖥` 이모지는 리눅스 컨테이너 폰트에서 흐리게 보였으나,
  실사용 환경(Windows/Segoe UI Emoji)에서는 정상 표시 예상. 기능 영향 없음.

### 남은 확인 (운영 환경에서 1회)
- [ ] 실제 Ollama + 실제 외부 API 키로 최종 연결 확인(목 서버가 아닌 실물).
- [ ] 실제 Qdrant + bge-m3 색인 상태에서 두 모드 답변 품질 비교.

---

## 향후 계획

### B. 이중 모드 후속 개선 (선택)
- [ ] 외부 모드에서도 사용자 예측 모델 Tool Calling 연동 동작 확인/보강.
- [ ] 외부 모드 모델 선택 시 응답 지연·비용에 대한 안내(선택적 UI 텍스트).
- [ ] 서버 로그에 외부 전송 이벤트 감사 로깅(보안 검토 필요).

### C. 기존 백로그 (SRS 6장 / CLAUDE.md)
- [ ] **BL-01 대화기억 영구보관**: `chat_engine.py`의 `_HISTORY`(서버 메모리) → 파일/DB 교체. 재시작 시 소실 해결.
- [ ] **BL-02 브라우저 테마·세션 저장**: 다크모드/세션을 localStorage 등에 저장(폐쇄망 보안 검토 후).
- [ ] **FR-TOOL-04 로컬 모델 Tool Calling 안정성**: docstring 정교화, 재시도 처리.
- [ ] **BL-03 답변 중단 시 서버측 생성 즉시 중지 검증**: GeneratorExit 처리 후 Ollama 생성이 실제로 멈추는지 확인.
- [ ] **BL-04 다크모드 잔여 라이트 스타일**: `.btn`·`.input-wrap`의 `background:#fff`
      하드코딩을 CSS 변수로 교체. (2일차 검증 중 발견, 선재 이슈)

---

## 참고: 현재 브랜치·상태
- 작업 브랜치: `claude/rag-agent-compress-commit-2bwx5x`
- SRS 최신 버전: **v4.2** (A 검증 결과는 변경 이력 v4.2.1에 기록)
- 검증 재실행: `python tests/verify_llm_modes.py` (자세한 내용은 `tests/README.md`)
- 다음 착수 지점: **B. 이중 모드 후속 개선** 또는 **C. 기존 백로그** 중 선택.
  간단히 정리하고 싶다면 **BL-04(다크모드 스타일)**가 가장 가벼움.
