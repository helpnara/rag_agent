# 사내 문서 RAG 에이전트 — 개발 요구사항 정의서 (SRS)

| 항목 | 내용 |
|------|------|
| 문서명 | 사내 문서 RAG 에이전트 개발 요구사항 정의서 |
| 문서 버전 | v4.1 |
| 최초 작성일 | 2026-07-06 |
| 최종 수정일 | 2026-07-06 |
| 상태 | 개발 진행 중 (프로토타입 반복 고도화) |

> **문서 목적**: 본 문서는 사내 문서 기반 RAG 에이전트의 요구사항을 정의하고, 반복 개발 과정의 요구사항 변경 이력을 추적·관리한다. 요구사항 변경이 발생할 때마다 본 문서를 갱신한다.

---

## 1. 개요 (Overview)

### 1.1 목적
특정 폴더에 있는 사내 문서(PDF, xlsx, pptx, txt 등)를 벡터화하여, LLM과 연동한 검색·질의응답이 가능한 웹 기반 시스템을 구축한다. 일반적인 RAG 기반 에이전트 구축 절차를 따르며, 사용자가 별도로 개발하는 예측 모델을 Tool Calling으로 연동하는 기능을 포함한다.

### 1.2 배경 및 제약
- **보안 요구**: 사내 정보 유출 방지를 위해 문서·질의 데이터가 외부로 전송되지 않아야 한다. 사내망은 외부 레지스트리 접속이 차단된 **완전 폐쇄망**으로 확인되었다.
- **실행 환경**: GPU가 없는 일반 사무용 PC(Intel i5-1135G7, RAM 16GB)에서 구동 가능해야 한다.
- **운영체제**: Windows 기반 환경에서 동작해야 한다(개발/검토는 Linux, 실사용은 Windows).

### 1.3 대상 사용자
사내 연구·실무 담당자. 문서 검색 및 질의응답, 문서 관리(업로드)를 수행한다.

---

## 2. 시스템 아키텍처 (Architecture)

```
[문서 폴더] → 로더 → 청킹 → 임베딩(로컬) → 벡터DB(로컬)
                                                  ↓
사용자 질문 → 웹UI → 백엔드 → LLM(로컬, Tool Calling) → [문서검색 | 사용자 모델] → 응답(스트리밍)
```

### 2.1 기술 스택 (최종 확정)

| 계층 | 기술 | 비고 |
|------|------|------|
| 웹 프레임워크 | FastAPI + Uvicorn | REST + SSE |
| 프론트엔드 | 단일 HTML/CSS/JS | 외부 CDN 미사용(폐쇄망) |
| 오케스트레이션 | LangChain | |
| 벡터DB | Qdrant | 로컬 Docker |
| 임베딩 | bge-m3 (로컬) | 1024차원, CPU 실행 |
| LLM | Ollama (qwen2.5:7b 등) | 로컬 실행, 사용자 선택 |
| 문서 로더 | PyMuPDF, openpyxl, python-pptx | 형식별 |

### 2.2 스택 변경 이력
- **v1 (초기 제안)**: LangChain + Qdrant + FastAPI + **Claude API** + **OpenAI 임베딩**
- **v2 (보안 요구 반영)**: LLM을 **Ollama 로컬 모델**로, 임베딩을 **로컬 bge-m3**로 전환. 외부 API 완전 제거.

---

## 3. 기능 요구사항 (Functional Requirements)

> ID 체계: `FR-<분류>-<번호>` · 상태: ✅완료 / 🔲예정 / ⏸보류

### 3.1 문서 처리 (Ingestion)

| ID | 요구사항 | 우선순위 | 상태 | 도입 |
|----|----------|----------|------|------|
| FR-ING-01 | PDF, xlsx, pptx, txt, md 파일을 형식별로 로드하여 텍스트를 추출한다 | 필수 | ✅ | v1 |
| FR-ING-02 | 추출 텍스트를 청크 단위로 분할한다 (기본 800자, 중첩 150자) | 필수 | ✅ | v1 |
| FR-ING-03 | 청크를 임베딩하여 벡터DB에 적재한다 | 필수 | ✅ | v1 |
| FR-ING-04 | 문서 메타데이터(파일명, 페이지/시트/슬라이드)를 함께 저장한다 | 필수 | ✅ | v1 |
| FR-ING-05 | 폴더 전체를 일괄 재색인할 수 있다 | 필수 | ✅ | v1 |
| FR-ING-06 | 업로드된 단일 파일을 자동으로 색인한다 (업로드 즉시 벡터화) | 필수 | ✅ | v4 |
| FR-ING-07 | 동일 파일명 재업로드 시 기존 벡터를 삭제 후 재적재한다 (중복 방지) | 필수 | ✅ | v4 |

### 3.2 검색 및 질의응답 (Retrieval & Chat)

| ID | 요구사항 | 우선순위 | 상태 | 도입 |
|----|----------|----------|------|------|
| FR-CHAT-01 | 질문과 관련된 문서를 유사도 검색한다 (기본 top-k=4) | 필수 | ✅ | v1 |
| FR-CHAT-02 | 검색된 문서를 근거로 LLM이 한국어로 답변한다 | 필수 | ✅ | v1 |
| FR-CHAT-03 | 답변에 사용한 출처를 표시한다 | 필수 | ✅ | v1 |
| FR-CHAT-04 | 답변을 실시간 스트리밍(SSE)으로 표시한다 | 필수 | ✅ | v2(web) |
| FR-CHAT-05 | 출처 문서를 카드로 표시하고, 클릭 시 원문 조각을 펼친다 | 권장 | ✅ | v2(web) |
| FR-CHAT-06 | 세션별 대화 맥락을 기억한다 (최근 8턴) | 권장 | ✅ | v2(web) |
| FR-CHAT-07 | 답변을 마크다운(굵게/목록/표/코드)으로 렌더링한다 | 권장 | ✅ | v2(web) |
| FR-CHAT-08 | 대화를 초기화할 수 있다 (새 대화) | 권장 | ✅ | v2(web) |
| FR-CHAT-09 | 생성 중인 답변을 중단할 수 있다 | 권장 | ✅ | v4 |
| FR-CHAT-10 | 답변을 클립보드로 복사할 수 있다 | 권장 | ✅ | v3(web) |

### 3.3 Tool Calling (사용자 모델 연동)

| ID | 요구사항 | 우선순위 | 상태 | 도입 |
|----|----------|----------|------|------|
| FR-TOOL-01 | 사용자가 개발한 예측 모델을 도구로 등록한다 | 필수 | ✅ | v1 |
| FR-TOOL-02 | LLM이 필요 시 사용자 모델에 입력값을 전달하고 결과를 답변에 포함한다 | 필수 | ✅ | v1 |
| FR-TOOL-03 | 문서 검색 자체를 도구로 제공한다 | 필수 | ✅ | v1 |

> **비고**: v1(에이전트 방식)에서는 LangChain `create_tool_calling_agent`로 구현. web 버전에서는 스트리밍 지원을 위해 "검색 후 생성" 구조로 재구성하고, 사용자 모델은 키워드 기반 간이 호출로 연동. 로컬 모델의 tool calling 안정성 보강은 추후 과제(FR-TOOL-04, 예정).

### 3.4 웹 UI

| ID | 요구사항 | 우선순위 | 상태 | 도입 |
|----|----------|----------|------|------|
| FR-UI-01 | 좌측에 RAG 대상 파일 목록을 표시한다 | 필수 | ✅ | v1(web) |
| FR-UI-02 | 우측에 대화형 채팅 인터페이스를 제공한다 (Anthropic 스타일) | 필수 | ✅ | v1(web) |
| FR-UI-03 | 파일 목록에 형식 배지, 크기, 수정일을 표시하고 미지원 형식을 구분한다 | 권장 | ✅ | v1(web) |
| FR-UI-04 | 웹에서 직접 문서를 업로드할 수 있다 | 권장 | ✅ | v3(web) |
| FR-UI-05 | 라이트/다크 모드를 전환할 수 있다 | 권장 | ✅ | v3(web) |
| FR-UI-06 | 사용할 LLM 모델을 콤보박스에서 선택할 수 있다 | 권장 | ✅ | v3(web) |

---

## 4. 비기능 요구사항 (Non-Functional Requirements)

| ID | 요구사항 | 상태 | 도입 |
|----|----------|------|------|
| NFR-SEC-01 | 문서·질의 데이터가 외부로 전송되지 않아야 한다 (완전 로컬) | ✅ | v2 |
| NFR-SEC-02 | 외부 API 키 없이 동작해야 한다 | ✅ | v2 |
| NFR-SEC-03 | 폐쇄망에서 최초 다운로드 없이 오프라인으로 동작해야 한다 | ✅ | v2(offline) |
| NFR-SEC-04 | 임베딩 모델은 로컬 폴더에서만 로드하고, 허브 접속을 차단한다 | ✅ | v2(offline) |
| NFR-SEC-05 | 업로드 파일은 경로 조작을 방지하고 허용 확장자만 저장한다 | ✅ | v3(web) |
| NFR-ENV-01 | GPU 없는 CPU 환경에서 동작해야 한다 (DEVICE 설정으로 전환) | ✅ | v2 |
| NFR-ENV-02 | Windows 환경에서 설치·구동 가능해야 한다 | ✅ | v1 |
| NFR-DEP-01 | 프론트엔드는 외부 CDN·라이브러리 없이 단일 파일로 동작한다 | ✅ | v1(web) |

---

## 5. 배포 및 운영 요구사항 (Deployment)

| ID | 요구사항 | 상태 | 도입 |
|----|----------|------|------|
| DEP-01 | 파이썬 패키지를 오프라인(wheel)으로 설치할 수 있다 | ✅ | offline |
| DEP-02 | 임베딩 모델을 반입 PC에서 받아 폐쇄망으로 이전할 수 있다 | ✅ | offline |
| DEP-03 | Ollama LLM 모델을 폴더 복사로 이전할 수 있다 | ✅ | offline |
| DEP-04 | Qdrant 도커 이미지를 save/load로 이전할 수 있다 | ✅ | offline |
| DEP-05 | 오프라인 구동 준비상태를 자동 점검하는 스크립트를 제공한다 | ✅ | offline |

---

## 6. 보류·예정 과제 (Backlog)

| ID | 요구사항 | 상태 | 비고 |
|----|----------|------|------|
| BL-01 | 대화 기억의 영구 보관 (파일/DB) | ⏸보류 | 현재 서버 메모리 저장, 재시작 시 소실. `chat_engine.py`의 `_HISTORY` 교체 필요 |
| BL-02 | 브라우저에 테마·세션 저장 (localStorage) | ⏸보류 | 현재 세션 동안만 유지, 새로고침 시 초기화 |
| FR-TOOL-04 | 로컬 모델 Tool Calling 안정성 보강 | 🔲예정 | docstring 정교화, 재시도 처리 |
| BL-03 | 답변 중단 시 서버측 LLM 생성 즉시 중지 보장 | 🔲예정 | 현재 GeneratorExit로 정리하나 검증 필요 |

---

## 7. 변경 이력 (Revision History)

| 버전 | 일자 | 주요 변경 내용 | 관련 요구사항 |
|------|------|----------------|----------------|
| v1.0 | 2026-07-06 | 초기 RAG + Tool Calling 프로토타입. LangChain+Qdrant+FastAPI, Claude API, OpenAI 임베딩 | FR-ING-01~05, FR-CHAT-01~03, FR-TOOL-01~03 |
| v1.1 | 2026-07-06 | Windows 실행 환경 가이드 추가 | NFR-ENV-02 |
| v2.0 | 2026-07-06 | **보안 요구 반영**: 완전 로컬 전환. LLM→Ollama, 임베딩→bge-m3, 외부 API 제거 | NFR-SEC-01~02, NFR-ENV-01 |
| v2.1 | 2026-07-06 | **폐쇄망 반입 절차** 및 오프라인 강제·점검 스크립트 추가 | NFR-SEC-03~04, DEP-01~05 |
| v2.2 | 2026-07-06 | CPU 환경 LLM 모델 선정 (qwen2.5:7b 권장) | NFR-ENV-01 |
| v3.0 | 2026-07-06 | **웹 UI 구축**: 좌측 파일목록 + 우측 채팅(Anthropic 스타일) | FR-UI-01~03 |
| v3.1 | 2026-07-06 | 채팅 고도화: 스트리밍, 출처 미리보기, 대화기억, 마크다운 | FR-CHAT-04~08 |
| v3.2 | 2026-07-06 | 문서 업로드, 다크모드, 답변 복사, 모델 선택 콤보박스 | FR-UI-04~06, FR-CHAT-10 |
| v4.0 | 2026-07-06 | 업로드 시 자동 색인(중복 방지 포함), 답변 중단 버튼 | FR-ING-06~07, FR-CHAT-09 |
| v4.1 | 2026-07-06 | 개발 환경을 Claude Code로 이전. 프로젝트 컨텍스트 파일(CLAUDE.md) 추가, 요구사항 정의서를 프로젝트에 포함 | 문서/DEP |

---

*본 문서는 요구사항 변경 시 지속 갱신된다. 신규 요구사항은 해당 분류에 ID를 부여해 추가하고, 변경 이력(7장)에 버전과 함께 기록한다.*
