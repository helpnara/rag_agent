# === 온프레미스(사내 폐쇄망) 의존성 ===
#
# 실제 사내 운영 구성. FastAPI 웹서버 + 로컬 Ollama + 로컬 bge-m3 + Qdrant(Docker).
#     pip install -r requirements-onprem.txt
#
# ▶ 공개 데모(Streamlit Cloud)는 저장소 루트의 `requirements.txt`를 사용한다.
#   Streamlit과 FastAPI는 starlette 버전이 충돌하므로 한 환경에 함께 설치하지 않는다.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# LangChain
# langchain 0.3.7 / langchain-community 0.3.5는 langsmith<0.2.0을 요구해
# 아래 langsmith==0.4.31 고정과 충돌한다(ResolutionImpossible). 0.3.27부터
# langsmith 상한 제약이 풀려 호환되므로 langchain·langchain-community를 함께 올린다.
langchain==0.3.27
langchain-community==0.3.27
langchain-huggingface==0.1.2
langchain-ollama==0.2.1
langchain-qdrant==0.2.0
# 외부 LLM API 모드(OpenAI 호환). 로컬 전용으로 쓰면 설치되어도 호출되지 않음.
langchain-openai==0.2.9
# langchain 0.3.27은 langchain-core>=0.3.72를 요구하는데, langchain-core 0.3.83부터
# uuid-utils(네이티브 모듈)가 새로 추가돼 아래 langsmith 고정과 같은 이유로 문제가
# 된다. uuid-utils가 없는 마지막 버전(0.3.82)으로 명시 고정한다.
langchain-core==0.3.82
# langsmith(추적 기능, 이 프로젝트는 사용하지 않음)는 langchain-core의 전이 의존성이다.
# 0.5+ 는 uuid-utils, 0.6.7+ 는 xxhash 같은 네이티브 모듈을 추가로 끌어오는데,
# Windows 애플리케이션 제어 정책이 이런 DLL을 차단하면 임포트 단계에서 죽는다.
# 네이티브 의존성이 가장 적은 0.4.x 로 고정한다. (폐쇄망 반입 wheel도 줄어든다)
langsmith==0.4.31

# Vector DB
qdrant-client==1.12.1

# 로컬 임베딩 (bge-m3)
sentence-transformers==3.3.1
huggingface_hub==0.26.2

# 문서 로더
pymupdf==1.24.13
openpyxl==3.1.5
pandas==2.2.3
python-pptx==1.0.2
tabulate==0.9.0
